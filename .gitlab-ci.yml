default:
  image: docker/compose

stages:
  - build
  - deploy

variables:
  DOCKER_DRIVER: overlay2


build:
  stage: build
  script:
    - docker compose build
  tags:
    - main


deploy:
  stage: deploy
  script:
    - bash set_env.sh
    - docker compose up -d
  tags:
    - main
